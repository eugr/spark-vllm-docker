# Recipe: Nemotron-3-Nano-NVFP4
# Nemotron-3-Nano model with NVFP4 quantization support
# Currently can only be run in solo mode, cluster mode fails with error

recipe_version: "1"
name: Nemotron-3-Nano-NVFP4
description: vLLM serving Nemotron-3-Nano-NVFP4 on a SINGLE NODE ONLY!

# HuggingFace model to download (optional, for --download-model)
model: nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4

# Container image to use. Modernized 2026-06-08 to the 0.22.1rc1 engine
# (vllm-node-eugr022, shared with Super/Ultra) off the old vllm-node (0.18.1).
container: vllm-node-eugr022

# This model can only run on single node (solo)
solo_only: true

# Nano's reasoning parser (nano_v3) is NOT built into vLLM 0.22.1 (verified) — this mod
# wgets nano_v3_reasoning_parser.py for --reasoning-parser-plugin below. (Nano has no MTP
# draft layers, so no speculative decoding here — only Super/Ultra carry MTP.)
mods:
  - mods/nemotron-nano

# Default settings (can be overridden via CLI)
defaults:
  port: 8000
  host: 0.0.0.0
  tensor_parallel: 1
  gpu_memory_utilization: 0.7
  max_model_len: 262144

# The vLLM serve command template
command: |
  vllm serve nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4  \
     --moe-backend cutlass \
     --max-model-len {max_model_len} \
     --port {port} --host {host} \
     --trust-remote-code \
     --enable-auto-tool-choice  \
     --tool-call-parser qwen3_coder \
     --reasoning-parser-plugin nano_v3_reasoning_parser.py \
     --reasoning-parser nano_v3  \
     --kv-cache-dtype fp8  \
     --enable-prefix-caching  \
     --load-format safetensors  \
     --gpu-memory-utilization {gpu_memory_utilization}
